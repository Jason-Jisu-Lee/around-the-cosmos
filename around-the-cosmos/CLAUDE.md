# Around the Cosmos

Celestial idle/incremental game (the protagonist/center object is named **Maw**).
Pure vanilla JS + Canvas. No build step, no framework, no dependencies.

## What this game is
- The **Maw** (small dark center) **auto-generates stardust (✦) every second** once you buy **Cosmic Pulse**, and is orbited by **orbiters** that pay stardust each orbit
- **This is an idle game, NOT a clicker** - clicking never harvests. A click only **catches a passing comet** (the one interactive moment) or opens a body's info card
- You **start with 10 ✦** - just enough to buy the first Cosmic Pulse, which begins the income
- Tone: calm, meditative, slightly melancholy - "maw" means a gap or void

> **Current scope (`refine/v.3` - minimal base, rebuilding step by step):** The game
> starts with **10 ✦ and 0 orbiters** - the **Maw's pulse** is the core income; buy your first orbiter from there.
> **Economy note:** effects are **additive (fixed +amount/level), not doubling** - this keeps
> the scale from exploding. Multipliers are used sparingly (only Speed and Composition).
> Upgrade sections are now **per orbiter** (future tabs): **MAIN** / **DUST PARTICLES** / **ASTEROID** / **COMETS**.
> **MAIN:** **Cosmic Pulse** (8 levels, costs [10,50,120,250,500,750,1000,1300], **+5 ✦/sec per level** - Maw auto-generates every `PULSE_INTERVAL`=1s once owned),
> then **Pulse Surge** (was Star Grasp; after 6th Cosmic Pulse; 5 levels, costs [500,1000,1500,2200,3000], **+10 ✦ per pulse per level**),
> then **Deep Breath** (unlocks at `G.runDust ≥ 10000`; 8 levels, costs [2500,6000,13000,27000,55000,110000,220000,430000] - **every Nth pulse pays ×`deepBreathMult()`** (base ×2), with N = `deepBreathInterval()` = `13 − lvl` so lvl 1 → every 12th breath down to lvl 8 → every 5th; the multiplier is applied in the `simulation.js` pulse loop, NOT in `pulseValue()`; a Deep Breath also bounces harder (`deepBreath` click-FX, amp 0.26 vs 0.15) and flares Maw glow gold (`deepBreathFlash` in render.js) with a deeper tick `sfxDeepBreath`),
> then **Abyssal Breath** (unlocks at the same `G.runDust ≥ 10000`; **15 levels**, costs [3000,5000,8000,13000,20000,31000,49000,77000,120000,185000,290000,450000,700000,1100000,1700000] - **empowers Deep Breath, +0.2× to its multiplier per level** via `deepBreathMult()` = `2 + 0.2×lvl('abyssal')`, so Deep Breaths run **×2 → ×5** at max),
> then **Gravitational Pull** (an **advanced** upgrade - unlocks at `G.runDust ≥ 50000`, 50k stardust collected this universe; 5 levels, costs [12000,20000,40000,93000,200000], each level adds
> **+1% of total orbiter payout to every pulse**), and **Resonance** (see below). Pulse value =
> `pulseValue()` = (5×Cosmic Pulse + 10×Pulse Surge + 0.01×gravpull×(total orbiter payout)) × denserCoreMult, rounded - **then doubled on a Deep Breath** in the pulse loop.
> The orbiter-payout sum (`orbiterPayoutSum`) uses each orbiter's **phase-averaged** payout (`avgPayout`, e.g. the moon) so the pulse doesn't flicker with the lunar phase.
> **(The old per-click Star Touch/Star Grasp and the separate Pulse auto-clicker upgrade are gone - the pulse IS the game.)**
> **DUST PARTICLES:** **Dust Particle** (after 2nd Cosmic Pulse; **one-time buy** that creates the first particle,
> cost 100, **+10 base payout**), then **Dust Particle Count** (after the first; **+1 particle/level**, 4 levels →
> 5 total, costs [600,1500,4000,7500] - all share one orbit on ring 0), **Dust Particle Payout** (**+10 to each dust
> particle's payout per level**, 5 levels, costs [200,800,2500,5500,12000]) and **Dust Particle Speed**
> (effective 82%→164%, costs [250,1000,3000,6500,16000]). (Like the other orbiters, the count/payout/speed
> upgrades populate only **once the first Dust Particle is bought**.)
> **ASTEROID:** **Asteroid** (after 5k stardust collected this universe, `G.runDust ≥ 5000`; a **single body**, one-time cost 2000, **+50 base payout**,
> own clump on the wider ring 1, with drifting dust motes) with **Asteroid Payout** (**+50/level**, 5
> levels, costs [2500,5000,11000,23000,52000]), **Asteroid Speed** (costs [3000,6000,13000,28000,62000]),
> and its unique **Asteroid Composition** (5 tiers Rock→Iron→Gold→Ice→Crystal→Diamond, recolors it + payout ×[1,1.2,1.4,1.6,1.8,2.0] (+0.2 per reforge, base ×1 → ×2.0 at max),
> costs [3500,11000,26000,60000,135000]). **MOON:** **Moon** (after 32k stardust collected this universe, `G.runDust ≥ 32000`; a **single body**, one-time
> cost 10000, **+200 base payout**, own clump on the widest ring 2 - pale, round, the slowest orbit) with
> **Moon Payout** (**+200/level**, 5 levels, costs [15000,50000,150000,330000,600000]), **Moon Speed**
> (the upgrade runs 100%→200%, scaled by a **0.663 base factor** (slowed 15% from 0.78) → 66.3%→132.6%, costs [9000,18000,35000,60000,100000]),
> and its unique **Lunar Phases** (`moonphase`, 5 levels, costs [12000,28000,55000,90000,140000]). Payout swings with the
> phase by **default**: ×0.75 at new moon up to ×1.25 at full moon; Lunar Phases shifts that range up by ×0.10 on **both**
> ends per level (lvl 5 → ×1.25..×1.75). Visible waxing/waning on a `MOON_CYCLE`=16s loop.
> **Resonance** (in MAIN, unlocks at `G.runDust ≥ 50000` - the same gate as Gravitational Pull, so they appear together; 5 levels) is a **global ×payout on every orbiter**,
> +0.10/level (×1→×1.5), and lights Maw's faint glow.
> **Only dust particles use a count upgrade**; all other orbiters are single bodies. **COMETS:** Comet Charm exists but is **disabled** (`unlock:()=>false`);
> comets still pay windfalls. Prestige, remnants, evolution, etc. are all out.
> Earlier ideas removed: New Planet, per-planet Orbit Payout/Speed, the per-planet tab UI
> (will return once there are >5 orbiters), First Light.

## File structure
Scripts load in this order - each file can reference globals from earlier files freely.

| File | Contents |
|---|---|
| `index.html` | Shell, **slim header** (`#header`, just the right-side controls - **no game-title logo**, it was removed), **3-column layout** (`#main` flex row, both side columns 308px). **Left column (`#left-col`):** the stardust HUD (`#dust-hud`: big `#dust-amount`) at the **top**, then **`#main-upgrades`** - the **MAIN** upgrades (Cosmic Pulse / Pulse Surge / Gravitational Pull / Resonance) rendered as **plain cards with NO section header** (no "MAIN" label / arrow / collapse). The **`#observatory`** is a **draggable floating panel** defaulting to the **bottom-left** (`position:fixed; bottom:20px`; bottom-anchored → grows **upward** with more metrics; floating so its hover popups aren't clipped). **Right column (`#right-col`):** `#upgrades-list` (the **orbiter** sections - Dust/Asteroid/Moon/Comets, collapsible accordions), with "Hide completed" pinned **bottom-right**. `buildPanels` (panels.js) renders the MAIN section as bare cards → `#main-upgrades`, every other section as an accordion → `#upgrades-list`, then **`appendPrestigeCards(mainList)`** pins the prestige entries at the very **bottom of the MAIN column** (no longer floating buttons): **`#accretion-btn`** (`.accretion-card` - a **special accentuated card**, dusty-blue `--accretion` tint + accent border + glow; opens `#acc-confirm`; inner `<span class="acc-card-label">Accretion</span>` + a `.acc-card-prog` bar. `hud.js`: **`.claimable`** (full accent card, clickable) when `canAccrete()`; **`.progress`** (a muted, non-interactive card with a thin **how-close-to-accreting** bar = `runDust / accrThreshold()`) shown **whenever not yet claimable** (`!claimable`, including before the first accretion - so the climb to the first accrete (~800k at the 2-Mass floor) is always visible) - nonintrusive) above **`#mass-upgrades-btn`** (`.mass-upg-card` - a smaller outlined secondary card; **shown after the first Accretion**, `G.massEarned > 0`; opens the Mass page in **browse/view-only mode**). Both keep their ids (so hud.js toggles their display) and are wired in panels.js. + `#accretion-screen` (the Mass page overlay) + `#accretion-fx` (full-window animation canvas) + `#acc-confirm` (top-level confirm modal) + **`#acc-leave-confirm`** (warns before leaving the post-Accretion Mass page, where Mass can be spent) + cosmo-tip/cosmo-card. Header has a **`#pause-btn`** (top-right, before mute) and there's a top-level **`#pause-overlay`** (dims everything + "PAUSED", click anywhere to resume). Also a full-window **`#vortex-layer`** canvas (z40, the Vortex event overlay) and the **`#comet-layer`** (z60). Loads every script below, in order (incl. **`event/vortex.js`** between comet.js and simulation.js). **Cache-busting:** `style.css` + every script tag carry `?v=N` - **bump N (currently 221) when you edit a CSS/JS file** or the browser may serve a stale cached copy. |
| `style.css` | All styling - parchment theme (#f4efe4 bg, Georgia serif, flat shapes). **The custom needle cursor is global** - a `--cursor` var (`url(assets/cursors/needle.png?v=N) 3 3`) on `html, body` plus an `!important` override on all interactive elements (buttons/inputs/labels/draggables) so it's NEVER the OS arrow/hand. `ui/cosmo.js` also sets `canvas.style.cursor` to the same needle **once** (guarded by `_cursorSet` - it's a fixed value and nothing else writes it, so re-setting it every frame was wasted style work). Themed `::selection` (gold) replaces the browser-blue highlight. The cursor URL carries its own `?v=N` cache-bust because the PNG is overwritten in place when regenerated. |
| `assets/` | Static assets + small self-contained design modules. `cursors/` holds `needle.png` (the in-game cursor - a **32×32 single flat ink** slim triangle pointing top-left, no outline, crisp supersampled edges) + `generate.js` (dependency-free pure-Node PNG generator - `node assets/cursors/generate.js`; the shape is the `TRI` triangle, tip = hotspot 3,3). `cursors`'s `assets/preview.html` shows it at 1×/4×/zoom. `click-effects/` holds `effects.js` (Maw click-reaction registry - see below) + `preview.html` (interactive gallery to feel all five). |
| **Core** | |
| `config.js` | CFG constants, PLANET_DEF (ring radii/periods), **PHYS** (cosmic-flavor physical model). Just constants. |
| `state.js` | G object, createInitialState (**starts with 10 ✦**), `newClump`, formatters (`fmtNum`/`fmtTime`/`fmtNice`/`fmtSci`), upg/lvl accessors, `pulseValue` (per-pulse income), **Maw physics** (`mawMass`/`mawGravity`/`mawEscapeVel`), earn, save/load |
| **`sound/`** | `core.js` (Web Audio engine: `SND` state, `audioBoot`/`tone`/`reverb`, volumes, mute. Signal chain: musicBus + sfxBus → master → **`SND.limiter`** (a `DynamicsCompressorNode` brick-wall limiter, threshold −2 dB / ratio 20 / attack 3ms / release 120ms) → destination - prevents overlapping sfx chords + loud volume settings from **clipping into static** at the output), `effects/sfx.js` (`sfxTap`/`sfxPulse`/`sfxDeepBreath`/`sfxOrbit`/`sfxComet`/`sfxBuy`/`sfxComplete`), `music/tracks.js` (3 procedural tracks + loop scheduler), `sound.js` (the `SoundSystem` facade everything calls) |
| **`orbiters/`** | `registry.js` (`ORBITERS`/`ORBITER_BY_ID`/`registerOrbiter`/`tipRow`); `dust.js`, `asteroid.js`, `moon.js`, `dwarf.js` - **one self-contained component per orbiter type** (body factory, payout/speed/velocity, **flavor description - edit here**, color/pebbleR/ring, list/clump/clumpPos, info-card `rows()`, buy-`labels`; `asteroid.js` also defines `ASTEROID_COMP`). **`dwarf.js`** is the **Dwarf Planet ("Ember")** on **ring 3** (the widest): a `sphere:true` orbiter rendered by `drawDwarfClump`/`drawDwarfSphere` in render.js (per-pixel procedurally-lit banded ball, `EMBER_TEX` cached once). **Constant speed** (`DWARF_SPEED = 0.663×1.2 = 0.7956`, the moon's speed at speed-level 1) - **no speed or count upgrade**, just Payout + **3 uniques** (Compounding Orbit / Trojan Companions / Conjunction - see the upgrade table). Uses two optional orbiter hooks added to the generic `tick` loop in simulation.js: **`onOrbit()`** (on top-cross → dwarf bumps `G.dwarfOrbits` for Compounding) and **`onTick(dt)`** (every tick → detects the Trojan companions' own top-crosses). **Conjunction** (`conjunctionBonus()`) is a flat +1000/lvl to both the dwarf and moon BASE payout. |
| **`upgrades/`** | `upgrades.js` - `UPGRADES` (all stardust upgrade defs: costs/effects/unlocks) + `SECTION_ORDER`. **`mass.js`** - the **Mass (prestige) upgrades** bought on the Accretion screen: `MASS_UPGRADES`/`MASS_BY_ID`, `mlvl(id)`, the effect accessors (`denserCoreMult`/`firstLightDust`/`heavierBodiesMult`/`brighterTailsMult`/`cometShowerMult`/`greaterCollapseMult`/`lunarFavorMult`), `buyMassUpgrade(id)`, and `applyMassUniverseStart()` (First Light starting ✦ + Retained Companions pre-owned bodies, applied at each universe's birth - **deterministic** (sets levels, not max) so Undo can reverse it). **Undo:** `accBuyLog` records this session's purchases; `undoLastMassUpgrade()` pops the last, refunds the Mass, and drops the level - repeatable back to the session start; `resetMassBuyLog()` clears it when a new post-Accretion session opens. Undo only touches **this session's** spending, never levels bought in prior accretions. **Tiered:** each node has a `tier` (0 unlocked from the start); a tier becomes buyable only once every node in the previous tier is maxed (`massCurrentTier(cat)` = first un-maxed tier; `massNodeVis(u)` → `'available'`/`'next'`/`'locked'`; `buyMassUpgrade` refuses non-available tiers). Real upgrades are tier 0; `placeholder:true` nodes (tier 1 = "next" preview, tier 2 = "locked") are scaffolding to demonstrate the tiers and will be replaced with real upgrades (their effects aren't wired yet). |
| `render.js` | canvas/ctx (the center `#sky`) + **`cometLayer`/`cometCtx`** (the full-window `#comet-layer` overlay) + `winMx/winMy` (window-space mouse), resize (sizes both canvases), orbitR, clumpPos/asteroidClumpPos/moonClumpPos, drawClump, **drawReticle(x,y,s,g=ctx)** (takes a target ctx), `mawScreen()` (Maw's window-space center, where comets aim), draw (iterates ORBITERS - **no longer draws the comet**), **`drawComet(t)`** (draws the comet + hover reticle/label + catch FX on the overlay, in **window coords**, so it flies over the side panels), burst, COMET_HOVER_R |
| `stars.js` | Background shining stars - `STAR_FRAMES`/`STAR_SEQ` (shine durations scaled by `STAR_SHINE_SCALE`=1.2, i.e. 20% slower fade in/out), `drawStars(t)` (called from `draw`). Animates ~22 twinkles from `assets/star/` frames, each with a per-star 75–125% size scale and a 120–140% per-star shine-duration scale (`sscale`). |
| **`comet/`** | `comet.js` - `randCometGap`, `spawnComet`, `catchComet`, `cometTick` |
| **`event/`** | `vortex.js` - **The Vortex**, a rare hold-to-claim background event (see the Vortex section). Self-contained like the comet: `VX` (tuning consts), `VTX` (state machine), `renderVortexBitmap`/`ensureVortexBitmap` (one-time cached procedural vortex - ported from `test/galaxy.html`), `pickVortexSpot` (random spot in the sky, outside the orbit system), `vortexSpawn`/`vortexTick`/`drawVortexLayer`/`vortexReward`, window-level press/hold handlers (registered at load, capture phase), `vortexInit`. Drawn on the full-window **`#vortex-layer`** overlay (z40). |
| `simulation.js` | `tick` (iterates ORBITERS + calls `cometTick`), `buyUpgrade` |
| **`ui/`** | `panels.js` (upgrade accordion + visibleSig), `observatory.js` (stats DOM + `updateObservatory`), `hud.js` (`updateUI` orchestrator), `cosmo.js` (hover tooltip + click-to-pin info cards, comet reticle label), `accretion.js` (**prestige flow + animation** - `#accretion-btn`→`#acc-confirm`→`startAccretionSequence` (commit Mass, freeze via `accreting`, `runAccretionFx` Implosion&Rebirth on `#accretion-fx`, then `resetUniverse`+`openAccretion`); the Mass page is the Tabs overlay `#accretion-screen` with category tabs Maw/Orbiters/Phenomena/Eternity (the tabs are the only category label now - the old per-tab title/subtitle were removed). `accTreeHTML` renders the **Branching Rail** - a CSS grid (`.acc-rail`, cols `gate / branch / cards`) iterating the 8 Singularity tiers: each row is `accGateHTML(t)` (the spine gate - `.acc-gate` shows **only the orbiter name** (no "Tier N"); state `done`(violet fill + **pressed-in inset shadow**, no "captured" text)/`avail`(violet border + glow, a **cost badge** top-left, `data-spine`)/`locked`(name = `???`)) + a dashed/solid `.acc-branch` + the tier's `.acc-tiernodes` (the tab's `accNodeHTML(u)` cards, or "— sealed —" if none). **The spine has its own MUTED VIOLET palette** (gate border `#8076a0`, captured fill `#6e6189`, rail+branch lines `#b0a3c8`) so it reads as a **different tree** from the gold upgrade cards; there is **no "Singularity" text label** (removed). **Cards are 2 rows** (title + one-line effect). Two **round corner badges `.acc-badge`** pinned on the corners: the **cost** (Mass number to buy next) on the **top-LEFT** of *every* button (`.acc-badge.cost`, gold when affordable; gates carry only this one), and the **level `X/Y`** (e.g. `1/3`) / `✓` maxed / `soon` planned / lock sealed on the **top-RIGHT** of upgrade cards only (Singularity gates have nothing on the right - single level). The hover `#acc-pop` is now **tiny**: just the plain-English flavour + a `Next: [next effect]` line (`accNodeDetail`, no title/level/cost), in **Nunito to match the stardust `#upg-pop` hover** (`#acc-pop` added to that font rule); the gate hover (`accGateDetail`) reads "**[orbiter] enters the orbit**". `accNodeHTML` node states: **locked** (tier's orbiter uncaptured → greyed/dashed box, no badge, **no hover**; the **name shows only for the NEXT tier** (`u.tier === singularityLevel()+1`) - deeper still-sealed tiers show a **lock icon instead of the title** (`.acc-nlock`) so you can't read ahead); **Planned** (`u.placeholder`, dim `.next`, `soon` badge, not buyable); else **max**/**up**/**avail**, with `data-id` + `.buy` only when affordable. `accRender` wires `.acc-node[data-id]`→`buyMassUpgrade` and `.acc-gate[data-spine]`→`buySingularity` (and skips the hover on **locked** nodes + locked gates). Each **gate box is the EXACT same size as a card** (208×116). Layout: the cards column stays **page-centred** (where the regular tree always sat) and the spine **hangs off to its left** - a 4-col grid `[gate 208][gap 100][cards auto][right-spacer 308]` where the spacer (= gate+gap) balances the spine so the cards land dead-centre (`.acc-rspacer` empty div per row; `.acc-railwrap` is `width:fit-content; margin:0 auto`). The rail **only connects captured tiers + the next available one** (`lineMax = min(singularityLevel()+1, tiers)`; only those gatecells get `.line` + a `.acc-branch`); **sealed tiers beyond have no rail line or branch** - revealed as you capture (`.acc-branch.noline` hides the connector). `.first`/`.last` trim the connected ends. See the Accretion section below) |
| `settings.js` | gear panel: volume sliders, track buttons, persistence |
| `debug.js` | initDebug, tickWithDebug (speed mult), dust inject / spawn comet / reset |
| `main.js` | Main loop (`gameClock` - the single animation clock `draw` uses, advances only while live), **pause** (`paused`/`setPaused`), canvas input (comet-catch + info cards), header controls, audio boot, `initDraggable` (drag-start pins pixel coords **and clears `transform`** so a centered panel like the debug panel doesn't lurch left by half its width), init |

No npm, no bundler, no TypeScript.

**`PROGRESSION.md`** is a player/designer-facing reference of the early-game flow and the
full upgrade structure (levels, costs, effects, unlock order). Keep it in sync with
`config.js`/`state.js` whenever balance or upgrade structure changes.

**Update this file whenever:** a new file is added, a mechanic changes, upgrade trees grow, state fields are added, CFG constants change, or the save key bumps.

## Key constants (CFG, top of config.js)
- MAX_PLANETS: 8 (only used to size `PLANET_DEF` / the `orbitR` ring math; dust particles use ring 0)
- COMET_MIN_GAP / COMET_MAX_GAP: 16–41s between comets
- COMET_LIFE: 8s on screen

## Upgrade tree (cost ✦) - defined in `upgrades/upgrades.js`. Effects are ADDITIVE, not doubling.
| id | Name | Levels | Effect | Unlock | Section |
|---|---|---|---|---|---|
| touch | Cosmic Pulse | 8 | Maw **auto-generates +5 ✦/sec per level** (income = `pulseValue()` earned every `PULSE_INTERVAL`=1s in simulation.js once `lvl('touch')≥1`; costs [10,50,120,250,500,750,1000,1300]) | always | MAIN |
| surge | Pulse Surge | 5 | **+10 ✦ per pulse per level** (adds to Cosmic Pulse via `pulseValue()`, costs [1000,1800,2800,4700,7050]; id renamed from legacy `grasp`, old saves migrated in `loadGame`) | after touch lvl ≥ 6 | MAIN |
| afterglow | Afterglow | 5 | **active-play bonus**: for **60s** after catching a comet, every pulse gains **+20 × level** ✦ (catching another comet refreshes the 60s). Applied as a flat add in the `simulation.js` pulse loop (`afterglowActive()` + `20×lvl('afterglow')`), NOT in `pulseValue()` and NOT multiplied by Deep Breath. The buff window is `afterglowUntil = gameClock + AFTERGLOW_DUR(60)` set in `comet.js catchComet`; `afterglowActive()`/`afterglowFrac()` live there too. Costs [2500,4500,8000,13000,22000] | after touch lvl ≥ 6 (with Pulse Surge) | MAIN |
| deepbreath | Deep Breath | 8 | **every Nth pulse pays ×`deepBreathMult()`** (base ×2; N = `deepBreathInterval()` = `13 − lvl`: lvl 1 → every 12th breath … lvl 8 → every 5th). Applied in the `simulation.js` pulse loop (not `pulseValue()`); the deep pulse bounces harder + flares the glow gold + plays `sfxDeepBreath`. Costs [2500,6000,13000,27000,55000,110000,220000,430000] | after `G.runDust ≥ 10000` (10k stardust collected this universe) | MAIN |
| abyssal | Abyssal Breath | 15 | **empowers Deep Breath**: `deepBreathMult()` = `2 + 0.2 × lvl` (×2.0 → ×5.0 at lvl 15), used by the pulse loop for the Deep Breath payout. Costs [3000,5000,8000,13000,20000,31000,49000,77000,120000,185000,290000,450000,700000,1100000,1700000] | after `G.runDust ≥ 10000` (alongside Deep Breath) | MAIN |
| gravpull | Gravitational Pull | 5 | each level adds **+1% of total orbiter payout to every pulse** (in `pulseValue()` via `orbiterPayoutSum()`, which uses **phase-averaged** payout so the pulse stays steady; costs [12000,20000,40000,93000,200000]). The card **desc** shows both the **current bonus** (✦/pulse at the owned level) and the **next-level bonus** (until maxed) | after `G.runDust ≥ 50000` (50k stardust collected this universe) - an advanced upgrade | MAIN |
| resonance | Resonance | 5 | **global ×payout on every orbiter**: `resonanceMult()`=`(1+0.10×lvl) × heavierBodiesMult()` - the Resonance term (×1→×1.5) times the **Heavier Bodies** Mass upgrade; applied in each orbiter's `payout()`. Maw glow reads `lvl('resonance')` directly (render.js), so it's unaffected by the Mass mult (costs [14000,27000,57000,150000,365000]) | after `G.runDust ≥ 50000` (alongside Gravitational Pull) | MAIN |
| dust | Dust Particle | 1 | **one-time** buy that creates the first dust orbiter (+10 base payout, ring 0); cost [100] | after touch lvl ≥ 2 (2nd Cosmic Pulse) | DUST PARTICLES |
| dustcount | Dust Particle Count | 4 | **+1** dust particle per level (particles 2–5; `count`=min(5, (dust≥1?1:0)+lvl(dustcount)), costs [600,1500,4000,7500]) | after dust lvl ≥ 1 | DUST PARTICLES |
| dustpay | Dust Particle Payout | 5 | **+10** to every dust particle's payout per level (`orbiterPayout`=10+10·lvl, costs [200,800,2500,5500,12000]) | after dust lvl ≥ 1 | DUST PARTICLES |
| dustspd | Dust Particle Speed | 5 | +20% per level; `dustSpeed()`=`0.82×(1+0.2×lvl)` → effective **82%→164%** (0.82 base factor, costs [250,1000,3000,6500,16000]) | after dust lvl ≥ 1 | DUST PARTICLES |
| asteroid | Asteroid | 1 | a single asteroid orbiter (+50 base payout, own clump on ring 1); one-time buy (cost [2000]) | after `G.runDust ≥ 5000` (5k stardust collected this universe) | ASTEROID |
| astpay | Asteroid Payout | 5 | **+50** to the asteroid's payout per level (`asteroidPayout`=(50+50·lvl)×comp, costs [2500,5000,11000,23000,52000]) | after asteroid lvl ≥ 1 | ASTEROID |
| astspd | Asteroid Speed | 5 | +20% per level; `asteroidSpeed()`=`0.88×(1+0.2×lvl)` → effective **88%→176%** (0.88 base factor, costs [3000,6000,13000,28000,62000]) | after asteroid lvl ≥ 1 | ASTEROID |
| astcomp | Asteroid Composition | 5 | **unique asteroid upgrade**: reforge Rock→Iron→Gold→Ice→Crystal→Diamond; recolors the asteroid (`ASTEROID_COMP.colors`) and ×payout [1,1.2,1.4,1.6,1.8,2.0] (+0.2 per level, base ×1 → ×2.0 at max; `asteroidPayout` is rounded → integer; costs [3500,11000,26000,60000,135000]) | after asteroid lvl ≥ 1 | ASTEROID |
| moon | Moon | 1 | a single moon orbiter (+200 base payout, own clump on the widest ring 2); one-time buy (cost [10000]) | after `G.runDust ≥ 32000` (32k stardust collected this universe) | MOON |
| moonpay | Moon Payout | 5 | **+200** to the moon's payout per level (`moonPayout`=round((200+200·lvl)×moonPhaseMult×reso), costs [15000,50000,150000,330000,600000] = dust-payout ratio ×100 (moon 10000 ÷ dust 100)) | after moon lvl ≥ 1 | MOON |
| moonspd | Moon Speed | 5 | +20% per level; `moonSpeed()`=`0.663×(1+0.2×lvl)` → effective **66.3%→132.6%** (0.663 base factor, slowed 15% from 0.78, costs [20000,80000,200000,385000,700000] = dust-speed ratio ×100) | after moon lvl ≥ 1 | MOON |
| moonphase | Lunar Phases | 5 | **unique moon upgrade**: shifts the moon's phase payout range up by **+0.10 on both ends per level**. `moonPhaseMult()`=`(0.75+0.10·lvl)+0.50·moonLit()` → default ×0.75 (new) .. ×1.25 (full); lvl 5 → ×1.25 .. ×1.75. Phase-avg mult = 1+0.10·lvl (`moonAvgPayout`). Costs [12000,28000,55000,90000,140000] | after moon lvl ≥ 1 | MOON |
| dwarf | Dwarf Planet | 1 | a single **Dwarf Planet ("Ember")** orbiter (+1500 base payout, own clump on the **widest ring 3**, slowest); one-time buy (cost [50000]). **Constant speed** = the moon's speed-level-1 (`DWARF_SPEED` 0.7956), no speed upgrade | after `singularityLevel() ≥ 1` (the **Singularity** spine tier 1 captures the Dwarf Planet) | DWARF PLANET |
| dwarfpay | Dwarf Planet Payout | 5 | **+1500** to the Dwarf Planet's payout per level (`dwarfBasePayout`=round((1500+1500·lvl)×resonanceMult), costs [50000,130000,320000,700000,1200000]) | after dwarf lvl ≥ 1 | DWARF PLANET |
| dwarfcompound | Compounding Orbit | 3 | **unique**: the Dwarf's payout ramps **+0.3%/orbit**, capped by level (`dwarfCompoundCap`=[15%,30%,50%]). `dwarfCompoundMult`=1+min(cap, 0.003×`G.dwarfOrbits`); ~58 min to hit +50%. **Resets each universe** (G.dwarfOrbits resets on accretion). Folded into `dwarfPayout`. Costs [120000,300000,700000] | after dwarf lvl ≥ 1 | DWARF PLANET |
| dwarftrojan | Trojan Companions | 2 | **unique**: each level spawns a Trojan companion at a **±60° Lagrange point** (max 2), drawn as smaller embers in `drawDwarfClump`. Each pays **1/3** the Dwarf's own payout (`dwarfTrojanPayout`=round(dwarfPayout/3)) on **its own** top-cross (detected in `dwarfOnTick` via `_crossedMark`). The dwarf's **displayed** payout (`dwarfAvgPayout` = dwarfPayout + count×trojanPayout) **folds the companions in** (so the info card / observatory read the combined income), but the bodies still earn separately. Costs [250000,700000] | after dwarf lvl ≥ 1 | DWARF PLANET |
| dwarfconj | Conjunction | 3 | **unique**: each level adds **+1000 to BOTH the Moon's and the Dwarf's BASE payout** (`conjunctionBonus()`=1000·lvl, added inside `dwarfBasePayout` and the moon's `(200+200·lvl + conj)` base via `moonConjBonus()`, guarded `typeof conjunctionBonus`). Costs [200000,500000,1000000] | after dwarf lvl ≥ 1 **and** moon lvl ≥ 1 | DWARF PLANET |
| charm | Comet Charm | 3 | comet windfall ×(1+0.25·lvl) (costs [30,80,200]) | **disabled** (`unlock:()=>false`) | COMETS |

`SECTION_ORDER` = `['MAIN','DUST PARTICLES','ASTEROID','MOON','DWARF PLANET','COMETS']` - **one section per orbiter** (each becomes a tab eventually); MAIN holds the pulse upgrades (Cosmic Pulse, Pulse Surge) + Gravitational Pull + Resonance. `buildPanels` renders each section as a
**multi-open accordion** (`.acc`): all sections open by default, each independently
collapsible (state in `sectionOpen`). A section with no shown cards is omitted.

> **Orbiters note:** dust particles share **one clump on ring 0** (the only orbiter with a
> **count** upgrade - `Dust Particle` creates the 1st, `Dust Particle Count` adds 4 more → 5 total);
> the asteroid is a **single body** on its own clump on ring 1
> (wider/slower, `PLANET_DEF[1]`); the moon is a **single body** on its own clump on the widest ring 2
> (`PLANET_DEF[2]`, slowest). All future orbiters are single bodies too. Each type has its
> own Payout (+amount/lvl) and Speed upgrade. The asteroid's unique upgrade is **Composition** (`astcomp`); the moon's is **Lunar Phases** (`moonphase`), which widens its default phase payout range.
>
> **Component architecture:** each orbiter is a self-contained component in `orbiters/` that calls
> `registerOrbiter(...)`. `simulation.js` (tick / orbit payout / **buy reconciles body count to `count()`** + float label), `render.js`
> (orbit rings + clumps), `ui.js` (combined All-Orbiters-Payout), `state.js` `loadGame` (rebuild bodies
> from levels), and the cosmic info cards all **iterate `ORBITERS`** - so adding an orbiter means adding
> a file in `orbiters/` (+ its upgrades in config.js + a body array/clump in state.js), not editing the
> engine. Each component exposes `list/clump/clumpPos/ring/hoverR/color/pebbleR/payout/speed/make/count/
> bodyUpgrade/rows/labels` (+ optional `avgPayout` for a phase-averaged display value, and `round` to
> draw as a phased disc - see `orbiters/registry.js` header). On any buy, `buyUpgrade` syncs the orbiter's
> body array to `count()` (push/pop), so both the "create first body" upgrade and a "+1 count" upgrade add bodies.

**Hide completed:** maxed upgrades are **hidden by default** - but only **once the Moon has ever been owned**
(`G.moonEverOwned`, a **persistent milestone** set in `buyUpgrade` when `moon` is bought; saved + kept across
Accretion resets). **Before that** the toggle is hidden (`hud.js` toggles `#upg-controls` display on `moonEverOwned`)
and completed cards are **never hidden** (`isShown` returns maxed when `!G.moonEverOwned`). The "Hide completed" toggle
(`#show-completed`, pinned **bottom-right** of the upgrade panel) is **UNCHECKED by default** - completed upgrades **show by default**; hiding is opt-in.
`showCompleted` defaults **true**; the change handler sets `showCompleted = !checkbox.checked` (checking "Hide completed" hides the greyed maxed cards).
`isShown(u)` returns maxed cards when `showCompleted || !G.moonEverOwned`;
the fingerprint `visibleSig()` includes max-state + the toggle + `moonEverOwned` so the panel rebuilds when an upgrade
maxes, the toggle flips, or the Moon is first bought. **Newly-unlocked cards** get a one-time **yellow double-flash** highlight
(`.upg-new`, CSS `@keyframes upg-flash`, ~0.9s, a yellow overlay that fades in/out twice then clears; an
`animationend` listener removes `.upg-new` so it returns to its default look): `seenUpg` tracks ids already
rendered; `firstPanelBuild` suppresses the flash for everything visible on the initial build (only mid-play unlocks animate).

Other mechanics:
- **Orbiters / payout**: three clumps (dust ring 0, asteroid ring 1, moon ring 2), rendered by the shared `drawClump()` helper (render.js).
  - **Dust** - `G.planets[]` (count = `dust`(0/1) + `dustcount`, capped 5), each `{localPhase,localR,localSpin,pulse,shape}`, travel as a clump on `G.clump{angle,nextTop}` (ring 0, period `PLANET_DEF[0].period`=6s). Speed = `(2π/period)×dustSpeed()`, `dustSpeed()`=`0.82 × dustspd.mult(lvl)` (mult=1+0.2×lvl) - a **0.82 base factor** (base speed reduced 18%) so effective speed runs **82% → 164%**. On top-cross each pays `orbiterPayout()` = `round((10 + 10·lvl(dustpay)) × resonanceMult())` (additive base × global Resonance; in orbiters/dust.js). Small grey pebbles (radius `PLANET_DEF[0].radius/3+2`), local orbit 5–12px.
  - **Asteroid** (single body) - `G.asteroids[]` (a **fixed** `ASTEROID_SHAPE` outline - rounded/lumpy, not the old per-load random spiky shape - + a `motes` array), clump on `G.asteroidClump` (ring 1, period `PLANET_DEF[1].period`=9.5s). Speed = `asteroidSpeed()`=`0.88 × astspd.mult(lvl)` (**0.88 base factor**, base reduced 12% → effective 88% → 176%). On top-cross pays `asteroidPayout()` = `round((50 + 50·lvl(astpay)) × ASTEROID_COMP.mult[lvl(astcomp)] × resonanceMult())` (rounded → integer; in orbiters/asteroid.js). Bigger pebble, radius `(PLANET_DEF[1].radius/3+4)×1.95`, local orbit 8–16px; **color = `asteroidColor()` = the current Composition tier** (`ASTEROID_COMP.colors`, Rock keeps `#7a6a55`). Carries 6 decorative **motes** - tiny specks that drift around it at all times (drawn in `drawClump` when `o.motes` exists; much smaller than a dust orbiter). Also gets **soft 2D shading for 3D roundness** (gated on `o.motes`): after the body is filled, the path is clipped and a **radial gradient** darkens the far side (transparent at the upper-left light, up to `rgba(20,16,12,0.34)` at the far edge). It's drawn in **world orientation** (`ctx.rotate(-la*1.5)` undoes the body spin) so the light stays fixed at upper-left while the rock tumbles. The asteroid **shape is unchanged** - it's just an overlay. **Dust pebbles get the same far-side shadow but much fainter** (the `else` branch in `drawClump`: same upper-left light + clip + world-orientation, gradient up to only `rgba(20,16,12,0.20)` vs the asteroid's 0.34) - just a touch of roundness.
  - **Moon** (single body) - `G.moons[]`, clump on `G.moonClump` (ring 2, period `PLANET_DEF[2].period`=13s - the slowest). **Sits exactly on the orbit line** (`localR:0` - no local wobble, unlike dust/asteroid). Speed = `moonSpeed()`=`0.663 × moonspd.mult(lvl)` (**0.663 base factor**, slowed 15% from 0.78 → effective 66.3% → 132.6%). On top-cross pays `moonPayout()` = `round((200 + 200·lvl(moonpay)) × moonPhaseMult() × resonanceMult())` (rounded → integer; in orbiters/moon.js). Largest body, radius `(PLANET_DEF[2].radius/3+6)×1.8`, color `#8f9498`. **Rendered by `drawRoundClump`/`drawMoonDisc` (render.js), not `drawClump`** (`o.round:true`), in the **"Studio"** style (from `test/moon-designs.html`): a **clean circular disc** (no rim ridges) filled with a **directional radial-gradient ball** lit from a fixed upper-left (`MOON_LX,MOON_LY`=−0.62,−0.55 → `MOON_LANG`), bright sub-light highlight `#d3d6d8` → mid `#9aa0a4`/`#6a7075` → dark far limb `#454b51` (the moon's flat `o.color()` is no longer used for the disc). Over it **crisp depth-shaded craters** (`MOON_CRATERS`, fixed → a tidally-locked face; each via `moonCrater()`: a dark floor + a lit rim arc toward `MOON_LANG` and a shadowed rim arc away from it). Then a **soft feathered lunar-phase terminator** - a semi-transparent `rgba(20,22,28,0.80)` shadow drawn at r×1.06 with a `blur(r×0.06)` canvas `filter` so the ball shading reads through it - waxing/waning on the `MOON_CYCLE`=16s loop (`moonPhase()`/`moonLit()`), and **limb darkening** (radial gradient, outer ~28%, up to `rgba(12,14,18,0.30)`) that rounds the flat disc into a sphere, finished with a **crisp 1px rim** (`rgba(60,64,70,0.35)`). **Payout rides the phase** via `moonPhaseMult()`=`(0.75+0.10·lvl(moonphase))+0.50·moonLit()` → default ×0.75 (new) .. ×1.25 (full), widened by Lunar Phases; the observatory uses `moonAvgPayout()` (mean mult 1+0.10·lvl) so stats don't flicker. No motes.
  - Background: **~22 shining stars** (decorative twinkles) drawn behind everything by `stars.js` `drawStars(t)` (called at the top of `draw`). Frames are the cleaned star animation in `assets/star/` (f1–f13, transparent); each star runs `STAR_SEQ` (the shine loop, durations ×`STAR_SHINE_SCALE`=1.2 so the fade in/out is 20% longer; blank pause after f3) with its own phase offset, a per-star **75–125% size scale**, and a per-star **120–140% shine-duration scale** (`sscale`, so they don't twinkle in lockstep), positioned by canvas-fraction and kept clear of the center. The white/gold source frames (built to glow on dark) are **re-tinted to warm gold on load** (`tintStarFrame`, luminance→`STAR_EDGE`..`STAR_CORE`) so they read on the parchment sky; disk assets stay untinted for dark screens. (Variation 1 - more star variations planned.)
- **Maw pulse reaction** (`assets/click-effects/effects.js`): each pulse briefly animates Maw **core only** (the glow stays put). `CLICK_FX` holds `pulseBeat` (the once-a-second "breath" - dur 0.62, a gentle swell with an eased rise and softer fall, `1+0.15·sin(π·p^0.72)`; **this is what the pulse uses** - simulation.js sets `clickFxId='pulseBeat'` and calls `triggerClickFx` each pulse), `deepBreath` (the **Deep Breath** upgrade's harder version of the same shape - `1+0.26·sin(...)`, dur 0.62; simulation.js sets `clickFxId='deepBreath'` on every Nth pulse) plus `bouncePop` (a quick uniform scale pop, kept in `CLICK_FX_LIST` for the debug/preview gallery). Each is `fn(p)` of progress `p`=0→1 over `dur`, returning `{sx,sy}`. `render.js` calls `clickFxTransform(t)` and draws the core as an **ellipse** (`CX+dx,CY+dy`, radii `sunR*sx`,`sunR*sy`) so squash/stretch works. (The debug-panel "Click FX" picker was **removed**; `clickFxRandom`/`clickFxId` still drive the pulse, and `assets/click-effects/preview.html` is still an interactive gallery.) `effects.js` is pure (no game globals) so the game + preview share it; it loads **before render.js** in index.html.
- **Maw center**: drawn at radius **13** (was 26 - shrunk 50%, will grow later). **No glow by default**; an irregular warm glow appears only once **Resonance** is bought - **three soft radial blobs** at gently drifting (time-based) offsets overlap into a non-circular, living haze (radius `sunR*6.72`), center alpha `(0.09 + 0.03×(lvl(resonance)-1)) × 1.1` (≈0.099 lvl 1 → 0.231 lvl 5). The blobs **drift and shimmer** (each blob's alpha + radius breathe over time via per-blob sine, `shimmer`) so the glow looks like it's radiating heat; the drift + offset blobs avoid a too-perfect circle.
- **Cosmic info** (`ui/cosmo.js` + the orbiters registry): `cosmoTargetAt(x,y)` returns `'comet'` (within `COMET_HOVER_R`=40px), `'maw'` (22px), or an **orbiter id** by iterating `ORBITERS` (each entry's `clumpPos()`/`hoverR`). Orbiter card titles/rows/descriptions come from `ORBITER_BY_ID[id]`; Maw card is built inline (`mawRows()` + `MAW_DESC`). The asteroid card includes a Composition (tier name) row. **Each orbiter card now shows a `Stardust / min` row** (`payout × orbits/min`; the moon uses its phase-averaged `moonAvgPayout()`). The **moon card** is gameplay-only now (Phase / Orbit payout / Orbits per min / Stardust per min) - the earlier physical stats (Class/Diameter/Mass/Surface gravity, and `PHYS.moonRadius`/`moonDensity`) were removed.
- **Hover targeting reticle**: hovering **any** entity (the comet, Maw, or any orbiter) shows a **targeting reticle** - a square drawn only at its corners (tactical-crosshair brackets, gently pulsing/breathing via `Math.sin(t*4)`, `drawReticle(x,y,spread)` in render.js, theme green-grey `rgba(60,80,70,…)`). At the end of `draw`, when `cosmoOver`, the reticle is placed by `cosmoTargetAt(cosmoMx,cosmoMy)`: **comet** → spread 18 (at `G.comet`), **maw** → spread 20 (at `CX,CY`), **orbiter** → spread `max(pebbleR()+8, 16)` at its `clumpPos()` (so it sizes to each body). The comet also shows a small **"Comet"** label (`.cosmo-solo`, the follow tooltip) and is **never pinned**; clicking it catches it (the `mousedown` comet-catch check runs before the open-card check).
  - **Hover → `#cosmo-tip`:** a small tooltip that **follows the cursor** (flips off the right/bottom edge), `pointer-events:none`, transient (hides on mouse-out). Hidden whenever a card is pinned.
  - **Click → `#cosmo-card`:** clicking a body opens its full card **pinned in the center of the sky**, `pointer-events:auto`, **sticky** (stays until dismissed) with a **× button** (top-right) / **Escape** (`closeCosmoCard`). Clicking a body opens the card (`mousedown` opens it on a target hit; clicking never harvests); **comets keep click priority** (a comet within 48px is caught first). The card is slightly transparent (rgba bg) and ~30% larger than the hover tooltip. **All expanded cards share a UNIFORM size** - `setUniformCardSize()` (ui/cosmo.js, run once lazily on first open) measures every body's card content and locks the card's `width` + `min-height` to the largest, so shorter cards just have empty space; it's re-derived each page load, so a longer description added later automatically grows every card. **The pinned card builds its shell (× button + `.cosmo-content`) once per open and rewrites only `.cosmo-content` each frame** - so live-updating cards (e.g. the moon's phase/payout) don't destroy the × button every frame (that used to make it flicker/unclickable). It also re-centers only when its size/viewport changes, not every frame.
  - **Content** (`ui/cosmo.js`): **Maw** - **Cosmic Pulse / s** + **Cosmic Pulse / min** (effective pulse income via `pulseIncomePerSec()` = `pulseValue() × deepBreathAvgMult() + (afterglow when active)`) and a concise `.tip-calc` breakdown line (e.g. `✦90/pulse × 1.80 Deep Breath + ✦100 Afterglow = ✦262/s`, only shown when Deep Breath is owned or Afterglow is active), then the flavor (`mawRows()` + `MAW_DESC`). The old physical stats (Diameter/Mass/Surface gravity/Escape velocity/Density) **and** the `mawMass`/`mawGravity`/`mawEscapeVel` helpers were removed. **orbiters** - title/`rows()`/`desc` pulled from `ORBITER_BY_ID[id]` (defined in each `orbiters/*` component).
  - Display uses **`fmtNice`** (≤2 decimals, integer ≥100). Orbiter cards show **Orbits / min** = the **in-game orbit cadence** (`60 × speed() ÷ PLANET_DEF[ring].period`, e.g. dust ≈8.2/min, asteroid ≈5.6/min, moon ≈3.06/min at base) - matches what you see and the observatory's payout/min. The **Maw card now shows pulse income** (Content above) instead of physical stats, so `PHYS.mawRadius`/`mawDensity` and `fmtSci` are currently unused (harmless dead config). innerHTML only rewrites when content changes (`_html` cache on each element).
- **Observatory stats** (`#stats-list`): (the **Cosmic Pulse / s** + **/ min** readout was moved OUT of the observatory into the **Maw info card** — see Cosmic info above); **All Orbiters Payout** + **All Orbiters Payout / min** (both after first orbiter - the /min = `Σ n×payout×60×speed÷PLANET_DEF[ring].period`, i.e. payout × the clump's orbits per minute); **Total income / min** (always - the whole passive economy: `60 × pulseIncomePerSec() + orbiterPerMin`, where `pulseIncomePerSec()` folds in the **Deep Breath average** and **Afterglow** (when active), so it tracks real income; `.stat-pop-row` with a breakdown popup); **Comet Value** (after the first comet is **caught**, `G.cometSeen` set in `catchComet`); **Vortex Value** (after the first vortex is **grabbed/absorbed**, `G.vortexSeen` set in `startAbsorb` — **NOT on spawn**, so the row appears only once you've claimed one; = the comet base value × `VX.REWARD_MULT`(4)); **both the Comet and Vortex popups are deliberately minimal, no numbers/formula** (unlike All Orbiters Payout / Total income, which keep the full calc): Comet = "Scales with pulse and orbiters.", Vortex = "Scales with comet value." (present-tense, no em dashes - see the copy-style note); **Total Stardust Collected** (always, `G.runDust` - current-universe total; hover popup explains it resets on prestige); **Time on Current Universe** (`universeTime`); **Total time played** (`gameTime`, the **last row** - a clear-time metric; **only appears after the first Accretion**, gated on `G.massEarned > 0` (the `showPlayed` flag → `'P'` in `statsSig`); cleared by the Reset button). The All Orbiters Payout, Comet Value, and Total Stardust Collected rows (`.stat-pop-row`) show a hover popup (`.stat-pop`). (There is no "Stardust / min" *stat row in the observatory*, but the **top HUD shows a small passive-income line** under the stardust count - `#dust-rate`, set in ui/hud.js to `Σ n×avgPayout×60×speed÷period` ✦/min, hidden via `:empty` until you own an orbiter. The old `G.income`/`incomeWindow` rolling-average machinery stays removed.) The DOM is built once per **row layout** (`buildStats`, keyed by `statsSig`) and values are updated in place each tick - so the hover popup doesn't flicker. Rows reset with the universe on prestige. **Display payout uses each orbiter's `avgPayout()` when present** (`(o.avgPayout||o.payout)()`) so the moon's phase-fluctuating payout shows a steady phase-averaged value in All Orbiters Payout / per-min / Comet Value - while actual orbit-cross earnings (simulation.js) stay live/instantaneous.
- **No free orbiter**: game starts with `planets: []` and **10 ✦**; Maw's pulse is the only income until you buy a Dust Particle (count = `dust`(0/1) + `dustcount`).
- **The pulse (core income)**: this is an **idle game - clicking never harvests**. Once **Cosmic Pulse** (`touch`) is owned, `simulation.js` `tick` runs a pulse loop: every `PULSE_INTERVAL`=1s Maw earns `pulseValue()`, plays a slow gentle **`pulseBeat`** bounce (effects.js - dur 0.62, eased rise/softer fall), and a soft non-intrusive **`sfxPulse`** tick (a low warm sine under the music). `pulseTimer` accumulates `dt` and drains in a `while` loop so it stays accurate under frame hitches. A module-level `breathCount` counts pulses; once **Deep Breath** (`deepbreath`) is owned, every Nth breath (N = `deepBreathInterval()`) is a **Deep Breath** - it earns `round(pulseValue() × deepBreathMult())` (base ×2, raised to ×5 by **Abyssal Breath**), swaps the bounce for the harder **`deepBreath`** FX, sets `deepBreathFlash = gameClock` (render.js draws a brief gold glow flare behind the core, independent of Resonance), and plays the deeper **`sfxDeepBreath`** tick instead of `sfxPulse`. **Afterglow** (`afterglow`, a MAIN active-play upgrade): catching a comet sets `afterglowUntil = gameClock + 60` (`comet.js`); while `afterglowActive()` (level > 0 AND within the window) each pulse adds a flat **+20 × level** ✦ (in the pulse loop, after the Deep-Breath calc, so it's not multiplied). **Display ("idea 1, on the card"):** `ui/panels.js updateCards` lights the **Afterglow card** (`.afterglow-on` warm-gold glow + a `.upg-drain` bar that empties over the 60s via `afterglowFrac()`; `.afterglow-low` burnt-orange flash in the final ~18%) — the card itself is the countdown, no separate HUD element. The buff timer rides `gameClock`, so it freezes with pause.
- **Clicking** (`main.js`): the comet rides a **full-window overlay** (`#comet-layer`, z60, `pointer-events:none`) in **window coordinates**, so it travels over the side panels and stays catchable there. A **window-level `mousedown` (capture phase)** catches it if the click is within `COMET_CATCH_R`=48px of the comet - **UNLESS** the click landed on a real control (`e.target.closest('button, input, label, .upgrade-card, .acc-node, #observatory, …')`), which wins (so clicking an upgrade that the comet is over buys the upgrade, not the comet). A successful catch `stopPropagation()`s to pre-empt the canvas handler. The **canvas `mousedown`** now only **opens a body's info card** (`cosmoTargetAt`→`openCosmoCard`) - comet catching moved to the window handler. `cosmoTargetAt` no longer hit-tests the comet (window-space now). No earning, no hold-to-autoclick.
- **Pause** (`#pause-btn`, top-right): `setPaused(p)` toggles `paused`. The main loop freezes when `paused || accreting` - it stops advancing **`gameClock`** (the single clock `draw()` receives) AND the sim, so income, orbits, comets, timers, AND every visual (twinkles, glow, the pulse bounce) freeze together. `#pause-overlay` dims the screen with "PAUSED" (z105; the header is z106 so its controls stay usable; clicking the overlay resumes). Music stops on pause, restarts on resume (and `_bootAudio` re-checks `paused` so a first-gesture-is-pause doesn't start music). simulation.js triggers the pulse FX off `gameClock` (not `performance.now`) so the bounce shares the clock.
- **Comet speed + windfall**: each comet spawns at a **random speed** `speedMult ∈ {1.0, 1.5, 2.0} × base` (`COMET_SPEEDS` in comet.js) - faster comets cross the screen quicker (harder to catch) but pay proportionally more. Windfall = `round((10 × pulseValue() + 1 × combined orbiter payout) × speedMult × brighterTailsMult())`, combined = `Σ list.length×payout()` over `ORBITERS`; sets `G.cometSeen`. (Base orbiter factor is **1** now, was 1.25; the speed multiplier (×1–2) is the new reward lever.) The observatory's **Comet Value** shows the **base (×1)** estimate. First comet ~7–13s in (then `COMET_MIN_GAP`–`COMET_MAX_GAP`, 16–41s).
- **No event overlaps** (`comet.js` `anyEventActive()` = `G.comet || VTX.active || BIGROCK.active`): comet/vortex (and the future giant-rock) each check `!anyEventActive()` before spawning, so only one event is ever on screen. A blocked spawn just waits (timer stays expired) until the screen clears.
- **The Vortex (event)** (`event/vortex.js`): a **rare hold-to-claim background event** (every **2.5–3.5 min** (~3 min avg), `VX.SPAWN_MIN/MAX`; the **first** vortex of a session is delayed an extra **+2 min** via `VX.FIRST_DELAY`(120) added to the initial `vortexTimer` only, so first ≈ **5–6 min**). A **small, distant** sepia spiral vortex (the recreation from `test/galaxy.html` - a per-pixel dust field rendered **once** to a cached bitmap via `renderVortexBitmap(VX.RENDER_R=150)`, then **scaled down** & rotated) appears at a **random spot in the sky, well OUTSIDE Maw's orbit system** (NOT centered on Maw - `pickVortexSpot()` keeps the whole disc clear of `orbitR(2)`+margin, within the `#sky` canvas bounds). On-screen disc radius is `min(W,H)×(0.085..0.125)` (small). The bitmap bakes the **exact sky cream `#f4efe4`** and the draw is **clipped to a circle**, so the disc is seamless over the parchment (no square). Rendered on the full-window **`#vortex-layer`** overlay (z40), **spinning up to max** (`VX.SPIN_MAX`). **State machine `VTX`** (driven by `vortexTick(dt)` from `simulation.js` → freezes with pause/accretion + obeys debug speed): `in` (**slow fade-in** + spin-up `VX.FADE_IN`=1.0s) → `stay` (**linger `VX.STAY`=5s - the grabbable window**) → `absorb` (success) or `out` (**slow fade out `VX.FADE_OUT`=2.0s**, not grabbable). **Total visible ≈ 1 + 5 + 2 = 8s.** No warning/telegraph phase and **no UI hand-holding** (no progress ring, no "press & hold" text - a dedicated tutorial is planned for later). **Mechanic:** press & **HOLD** the disc for `VX.HOLD`=3s to absorb it. Holding **freezes** the linger timer (`stayLeft` only counts down while NOT holding - so grabbing at 4.99s keeps it alive; releasing resumes the remainder and it expires fast) and progressively **collapses the spiral into the central void** (`VTX.shrink`, scale→0, spin accelerates ×up to 4.2) - **that visible shrink-into-the-void is the only feedback** (intuitive enough). Releasing before 3s **resets the hold and snaps the collapse back** (`shrink` eases to 0). Completing the hold → `startAbsorb()`: grants **`vortexReward()` = ×4 a comet's base value** (`round((10×pulseValue() + Σ list.length×payout()) × brighterTailsMult()) × VX.REWARD_MULT`), **sets `G.vortexSeen`** (this is the ONLY place it's set — gating the observatory's Vortex Value row to "after the first grab", not on appearance), a white-bloom/gold-shock flash, a rising `+✦` float (drawn on the overlay in window coords), and `sfxVortexAbsorb`. **Sounds** (`sound/effects/sfx.js` + facade): `sfxVortexAppear` (the **"Braam"** - a cinematic low-brass emergence: a detuned 9-voice sawtooth cluster at G1 (root 49Hz + fifth + octave) through a lowpass that opens 200→1450Hz then closes, over a sub-bass drop (64→34Hz) and a soft air rush, with a cached short hall reverb routed to `SND.sfxBus`; hand-built since `tone()` can't do the cluster/filter/reverb. Other candidates live in `test/vortex-sounds.html`) and `sfxVortexAbsorb` (the reward sound on a successful hold). **Input:** window-level `mousedown`/`mouseup`/`touch` (capture phase, registered at load so it pre-empts comet-catch/info-card; `stopImmediatePropagation` on a hit) - only claims clicks within the disc during `stay`, and yields to real controls. `drawVortexLayer()` (called each frame in `main.js`) renders it; the bitmap is **pre-rendered ~2.5s after load** (`vortexInit`) so the first spawn is instant. **Debug:** a **Spawn Vortex** button. Spawn timer is module-level (`vortexTimer`), not on `G`.
- **Number format**: `fmtNum` writes any number **< 100,000 in full, comma-grouped** (10000 → "10,000", whole - never a decimal); **6 figures+ abbreviate** as K/M/B/T, trailing zeros trimmed (100000 → "100K", 1e6 → "1M", 123456 → "123.46K"). The comet windfall and all orbiter payouts are rounded at the source so `G.dust` stays integral.
- **Upgrade cards** (`ui/panels.js updateCards`) - the **"Demo-3 / layout A"** treatment: a **top row** with the upgrade **name** (`.upg-name`, **Baloo 2** 16px - excluded from the global Nunito `!important` rule so the serif applies) on the left and the **cost** (`.upg-cost`, Nunito 800, tabular-nums) on the right (baseline-aligned); then a **bottom row** (`.upg-botrow`) with a **full-width dusty-blue level progress bar** (`.upg-bar > i`, width = `lvl/maxLevel`, set in `updateCards`) and the **level count** (`.upg-level`, e.g. `3 / 8`) at its right end. White cards on parchment; **affordable** cards get a dusty-blue border (`--accretion-dim` → `--accretion` on hover), **maxed** cards dim `.upg-top`/`.upg-botrow` (cost shows `MAX`, bar full). No left colour bar; the existing `.upg-new` yellow unlock-flash is kept. The hover description is a **single shared popup** (`#upg-pop`, slightly transparent) JS-positioned **to the left** of the hovered card (`showUpgPop`/`hideUpgPop` in panels.js wired via each card's `mouseenter`/`mouseleave`). The popup has **two areas**: an optional **flavor** line (`u.flavor`, italic/muted, the upper "description") and the effect text below (`u.desc(lvl)`) - separated by a divider, no label (`.upg-pop-flavor`/`.upg-pop-fn`). A maxed card's cost shows **`MAX`** (not an em dash). It's `position:fixed` so it escapes the right column's overflow clip and hides nothing in the list (a card-child popup would be clipped, since `overflow-y:auto` forces `overflow-x:auto`). Accordion arrows (`.acc-arrow`) are 15px accent for a clear collapse affordance. **Maxed cards stay greyed even on hover** - only the *face* (`.upg-top` + `.upg-botrow`) is dimmed (`opacity:0.4`) plus a muted bg/border, **not** the whole card; so the popup keeps full opacity and isn't trapped behind sibling cards (the old whole-card `opacity:0.3` did both, which is why it used to glow-on-hover). The right column is **308px** wide (~20% wider than the original 256px).

## State object (G)
Key fields: `dust`, `runDust`, `totalDust` (**lifetime stardust - never resets**; a lifetime stat — Mass is now earned from per-universe `runDust`, not this), `mass` + `massEarned` (Accretion meta-currency; `massEarned` is a lifetime tally / "ever-accreted" flag, no longer an anti-farm subtractor — see Accretion below), `massUpgrades{singularity,denserCore,heavierPulse,firstLight,heavierBodies,denseDust,lunarFavor,brighterTails,cometShower,greaterCollapse}` (**Mass upgrade levels - persist across every Accretion; kept by `resetUniverse` + save/load; seeded by `blankMassUpgrades()` in state.js so it exists before mass.js loads**), `moonEverOwned` (**persistent milestone** - true once the Moon is ever bought; survives Accretion; gates the Hide-completed toggle), `orbitsCompleted`, `dwarfOrbits` (**this-universe Dwarf Planet laps** - drives Compounding Orbit; resets each universe, saved), `taps` (vestigial - no manual clicking anymore), `cometsCaught`, `cometSeen` (caught a comet this universe → gates Comet Value stat), `vortexSeen` (a vortex has been **grabbed/absorbed** → gates the Vortex Value stat; set in `startAbsorb`, not on spawn; persists via save), `gameTime` (**total time played - shown in observatory; only the Reset button clears it, survives Accretion**), `universeTime` (current-universe timer; reset on prestige later), `upgrades{touch,surge,deepbreath,abyssal,afterglow,pulse,gravpull,dust,dustcount,dustpay,dustspd,asteroid,astpay,astspd,astcomp,moon,moonpay,moonspd,moonphase,dwarf,dwarfpay,dwarftrojan,dwarfconj,dwarfcompound,resonance,charm}` (**`pulse` is vestigial** - the Pulse auto-clicker upgrade was removed; the field stays for save compat but never levels), `planets[]` (dust particles, ring 0) + `asteroids[]` (asteroids, ring 1) + `moons[]` (moon, ring 2) + `dwarfs[]` (Dwarf Planet "Ember", ring 3) - each orbiter `{localPhase,localR,localSpin,pulse,shape}`, empty at start, rebuilt from `dust`/`asteroid`/`moon`/`dwarf` levels on load, `clump{angle,nextTop}` + `asteroidClump{angle,nextTop}` + `moonClump{angle,nextTop}` + `dwarfClump{angle,nextTop}` (the four shared orbits), `comet`, `particles[]`, `floatingTexts[]`

## Sound system (`sound/`)
Split into: `core.js` (Web Audio engine - the shared `SND` state object, `audioBoot`, `tone`, `reverb`, volumes, mute), `effects/sfx.js` (`sfxTap`/`sfxPulse`/`sfxOrbit`/`sfxComet`/`sfxBuy`), `music/tracks.js` (3 procedural tracks Celestial/Drift/Wane + loop scheduler), and `sound.js` (the `SoundSystem` facade everything calls). Internals are plain globals sharing the `SND` object - **not** an IIFE.
- `SoundSystem.boot()` - call on first user gesture (wired in main.js to the earliest gesture events `pointerdown`/`touchstart`/`mousedown`/`keydown`/`click`, so the very first interaction starts the music; the browser autoplay policy makes *some* first gesture unavoidable on a fresh page load). **The header RESET (`resetGame`) wipes localStorage + recreates state IN PLACE (no `location.reload`)** - so the audio context survives and music keeps playing through a reset (a reload would drop it and force a new gesture).
- `SoundSystem.startMusic()` / `setTrack(n)` - looping ambient music
- SFX: `sfxTap`, `sfxPulse` (the soft once-a-second pulse tick - low warm sine, plays on every Maw pulse in simulation.js), `sfxDeepBreath` (a **deeper, weightier sibling of `sfxPulse`** - pitched down a 4th (147/220 Hz) but the **same short length** and roughly the same loudness, so it stays non-intrusive; plays **instead of `sfxPulse`** on every Nth Deep Breath), `sfxOrbit` (the per-orbit payout - a **soft kalimba/marimba pluck**, a **random pentatonic note** (`ORBIT_PENT` C5–A5) each payout so a screen of orbiters becomes a gentle melody, not one repeated beep; built non-intrusive for ~9 orbiters, only a 0.05s same-frame-stacking cooldown), `sfxComet`, `sfxBuy`, `sfxComplete` (soft ascending chime played by `buyUpgrade` when an upgrade reaches max level; the normal `sfxBuy` blip plays otherwise), `sfxHover` (the **"Warm" upgrade-hover blip** - low 294→250 Hz sine under a 600 Hz low-pass, built by hand since `tone()` has no filter; **40 ms cooldown** so sweeps don't machine-gun. Played on `mouseenter` of every regular upgrade card (`ui/panels.js`) and every Mass node (`ui/accretion.js`)), `sfxVortexAppear` (the **"Braam"** - a cinematic low-brass blast as the Vortex tears in: a detuned saw cluster through an opening lowpass over a sub-bass drop + air rush + short hall reverb, hand-built and routed through the sfx bus so the limiter tames it), `sfxVortexAbsorb` (downward whoosh → warm boom → sparkle when the Vortex is absorbed for the windfall)
- Mute button (#mute-btn) in header toggles master gain

## Save system
localStorage key: `around_the_cosmos_v1` (migrates an old `lacuna_v1` save on load). Saves every 20s and on tab close. Settings: `around_the_cosmos_settings_v2` (volume scale rebased - see below; old `v1`/`lacuna_settings_v1` saves are **not** migrated so everyone lands on the new 75% default).

**Volume scale:** the music/sfx sliders run 0–100% but **75% is the reference loudness** (what used to be 100%); the extra range to 100% gives ~33% more headroom. `setMusicVolume`/`setSfxVolume` (sound/core.js) divide by **75**, not 100 (reference gains `0.5712*pct/75` music, `0.8262*pct/75` sfx). Defaults are **75%** (HTML slider value, `loadSettings`, the `_savedVols` boot fallback in main.js).

## Debug panel
URL: `?debug` (e.g. `localhost:3000?debug`)
Buttons to inject ✦ (100, 1K, 10K, 100K, 1M, 10M), set speed multiplier, force-spawn comets, **force-spawn the Vortex (Spawn Vortex)**, a **"Skip Acc Anim" toggle** (`debugSkipAcc`, debug-only: when ON even the first-ever accretion skips its animation - the player still cannot), reset. (The old "Click FX" picker was removed.) Draggable; **defaults to the bottom-centre** (`left:50%`) so it doesn't sit on top of the bottom-left Accretion button. Does NOT appear in normal play.

## Accretion (prestige) - flow + animation + Mass upgrade tree
**The flow** (`ui/accretion.js`): **`#accretion-btn`** (bottom-left, Dusty-Blue accent) toggles between two states in `hud.js`, both gated on **`runDust`** (the *current-universe* stardust shown as "Total Stardust Collected", **not** lifetime `totalDust`): a clickable **`.claimable`** card when **`canAccrete()`** (you'd gain at least `accFloor()` = **2** Mass, so ~**800k** runDust on a fresh save with the square-root curve), else a muted **`.progress`** card (`!claimable`, shown **from the start** including before the first accretion) whose thin bar tracks the climb toward that first accrete. So the card is effectively always present while not `accreting` (progress → claimable at ~800k). **Note:** `ACCRETION_THRESHOLD`=200k is just the per-Mass *unit*, **NOT** the button gate - the gate is `massEarnable() >= 2` (≈800k fresh, and `accrThreshold()` rises as lifetime Mass grows). Toggled via explicit `'block'` - `''` falls back to CSS `display:none`. Pressing it opens **`#acc-confirm`** directly (a top-level fixed modal, **themed Nunito/parchment** - not an OS warning box) - **not** the upgrade page. The modal is the **"ledger" design** (centred, show-don't-tell, no title/eyebrow): a **Resets ⟶ Mass gained** ledger (✦ Stardust / ⬡ Upgrades on the left; a Mass **orb** + `+N` on the right) - the two boxes are **equal-height with parallel headers** (`.acc-ledger` `align-items:stretch`, each `.acc-col` a flex column with a `.acc-colbody` that centres its content under the header), then a **centred two-line explanation UNDER it** ("All Stardust and orbiters collapse into Mass" / "Mass can be used for permanent upgrades" - the old "COLLAPSE THIS UNIVERSE" title was removed), and a **Next Mass** progress bar (`✦…to go`). Actions are **vertical**: a primary **Collapse** button with a quiet **Not yet** under it (`#acc-confirm-cancel` → `closeAccConfirm`). **Collapse is HOLD-to-confirm** (`initCollapseHold` in accretion.js): press & hold ~1s while a flurry of tiny stars gather into the button's centre (`.acc-go-fx`) - at 1s it fires `startAccretionSequence()`; releasing early disperses the stars and cancels (no click handler). On the **first-ever accretion** (`G.massEarned === 0`) the button label reads **"Collapse (Hold)"** (set in `openAccConfirm`); afterwards just **"Collapse"** - the only hint that it's a hold, no progress ring/extra FX. The secondary **"Not yet"** / leave-modal **"Not Yet"** are `.acc-btn-ghost` - a **visible outlined button** (not a faint text link); the leave-confirm box is widened so its note sits on **one line**. `accConfirmBody()` rebuilds the body each open. Its **tail** depends on whether you've accreted before (`G.massEarned`): **first Accretion ever** (`massEarned === 0`) → the animation always plays, and a **music-muted hint** (`.acc-mutehint`) shows only when music is silenced (`musicMuted()` = `SoundSystem.isMuted()` or `#vol-music` at 0); **after the first** (`massEarned > 0`) → a **"Skip animation" checkbox** (`#acc-skip-anim`, **UNCHECKED by default** = the collapse plays; check it to skip) - it lives **OUTSIDE the box entirely**, **top-left above the box** (`#acc-skip-outer.acc-skip`, a sibling **before** `.acc-confirm-box` inside `.acc-confirm-stack`; bright white text + strong shadow so it stands out on the dim backdrop). It's a permanent DOM node now (not regenerated in the body): `openAccConfirm` toggles its display (`massEarned > 0`) + resets the checkbox to **unchecked**. `startAccretionSequence()` computes `skipAnim = G.massEarned > 0 && checkbox.checked` **before** `commitAccretion()` bumps `massEarned` - so the **first-ever accretion ALWAYS plays the animation** regardless of the (hidden) checkbox. If `skipAnim` it **skips `runAccretionFx`** entirely - `commitAccretion` + `resetUniverse` + `openAccretion` with just a quick 0.4s fade to the Mass page (no animation, no accretion cue). Confirm → `startAccretionSequence()`:
1. `commitAccretion()` - **credits Mass immediately** (the major event happens at confirm, not later).
2. sets **`accreting=true`** (main.js loop skips `tickWithDebug` → sim/earning/comets frozen; **stays true through the whole Mass page**, only cleared on Return), clears the comet, dismisses any pinned info card, and **stops the music** (`SoundSystem.stopMusic()`; SFX are already silent since the sim is frozen), then plays the one-shot cue **`sound/music/accretion-1.mp3`** once (`accretionAudio`, an HTML5 `Audio`, vol 0.68, gated on `!SoundSystem.isMuted()`; stopped on Return).
3. `runAccretionFx()` plays a **Singularity collapse & Rebirth** on the full-window `#accretion-fx` canvas (z110, **opaque** - covers the game; centered on the on-screen Maw). It's **choreographed to `accretion-1.mp3` as 5 timed beats** (`FX_TL` = windup 15.0 / absorb 4.15 / collapse 2.55 / converge 10.0 / rebirth 2.5 s; ~34.3s total): **WINDUP** - orbiters (snapshotted as polar bodies via `o.clumpPos()`/`o.color()`, each seeded with its **real current angular speed** `(2π/PLANET_DEF[ring].period)×o.speed()`) spin up from their current speed to `SPIN_TOP`=14× on the bright parchment; **ABSORB** - they spiral into Maw while the background darkens (the starfield is hidden on the bright bg, alpha purely `dark`-driven, and fades in only here); **COLLAPSE** - white flash + gold shockwave (the flash lands at **19.15s**); **CONVERGE** - a 150-star field accelerates inward and **recycles** any swallowed star to the rim (endless universe) for the first ~6s, then over the last 4s the inflow **decelerates to a stop in place** (no outward push) and settles to a calm field, stars on Maw fading out; **REBIRTH** - purple bloom over the calm field. After rebirth it **holds the final frame for `FX_HOLD`=3s** (music still playing), then `onDone` runs `resetUniverse()`+`openAccretion()` and **fades the Mass page in over 3s** over the still-showing FX backdrop (the Mass page is z120 > FX z110), removing the FX canvas after the fade. The **accretion cue keeps playing** through the hold + fade and into the Mass page (only stopped on Return). Tunable timing lives in the throwaway lab `test/singularity-demo.html` (sliders per beat + Play+Music sync) - delete `test/` once locked.
4. on finish → `resetUniverse()` (full reset keeping `mass`/`massEarned`/`totalDust`/`gameTime`), then **`openAccretion()`** → the **Mass page** (`#accretion-screen`). It's **opaque parchment** (the game - Maw/orbiters/comets/panels/observatory/header - is fully hidden and stays frozen) with its own **`#acc-stars`** canvas running the **same looping stars** (`drawStars(t, g, w, h)`, now parametric in stars.js) so only "background colour + stars" show behind the UI. Tree is **top-aligned** (`.acc-stage justify-content:flex-start`) so it reads top-to-bottom without scrolling; big nodes (≈210px). The **bottom-center** button (`#acc-return`, larger, `--accretion` fill) plus a top-right **`#acc-undo`** ("↶ Undo", shown only in spend mode when `canUndoMass()`) are the only chrome - there is **no "Accretion" eyebrow/title** label. **Two modes** (`accBrowse` flag):
- **Post-Accretion mode** (this flow, `accBrowse=false`): opaque, game frozen, **Mass is spendable** (nodes clickable). `#acc-return` reads "Return to the Cosmos" and routes through `onAccBack` → `openAccLeaveConfirm()` (the **`#acc-leave-confirm`** modal warns that Mass can only be spent here, right after accreting - once you leave you can only *view* the tree until the next Accretion). Confirming → `closeAccretion()` (hides page, stops stars, `accreting=false`, restarts music → new universe resumes).
- **Browse mode** (`openMassBrowse()` from `#mass-upgrades-btn`, `accBrowse=true`): the **game keeps running** behind a **translucent** page (`.browse` class - `rgba(244,240,232,0.8)`, no `#acc-stars`; the real sky shows through), the tree is **view-only** (`accNodeHTML` drops the `data-id`/buy affordance when `accBrowse`), and the button reads **"Back"** → `closeMassBrowse()` (no confirm, no freeze, no music change).
Escape routes the same way (leave-confirm → acc-confirm → `onAccBack`). There is no in-page Accrete/× button.
- **Mass** (`G.mass`): `massEarnable()` = `floor(sqrt(runDust / massUnit()))`, where `massUnit()` = `ACCRETION_THRESHOLD × (1 + MASS_LIFE_K × G.massEarned)` = `200000 × (1 + 0.02×massEarned)` - **DIMINISHING (square-root) in runDust**, so pushing further still earns more but at a **slowing** rate (push 4× the stardust for ~2× the Mass) and totals stay small instead of running away. At zero lifetime Mass: 800k→2, 2M→3, 5M→5, 10M→7, 50M→15 (vs the old linear 10M→90). **Per-run** — reads **`G.runDust`** (resets each universe). **Difficulty scales with lifetime Mass:** `massUnit` grows with `G.massEarned` (each lifetime Mass = +2% runDust per Mass), so a richer save earns less from the same runDust (10M → 7 at life 0, 4 at life 80) and must push further to accrete. **Was linear (`MASS_COEF`=1.8); reverted because it handed out too much Mass too fast and never felt like it was tapering.** **Accrete floor:** you can only accrete once you'd gain at least **`accFloor()`** = **2** Mass (flat - no spammy 1-Mass prestige; the lifetime difficulty lives in `massUnit`, not the floor). `canAccrete()` = `massEarnable() >= accFloor()`; `accrThreshold()` = `massUnit() × accFloor()²` is the runDust to reach the floor (≈800k fresh, used by the progress bar). Threshold/exponent/floor/`MASS_LIFE_K` are tunable. Pairs with the design that **stardust upgrades are additive** while **Mass upgrades are multiplicative**. `massGain()` = `round(baseMassGain() × greaterCollapseMult())` (what's shown + granted). `commitAccretion()` adds the grant to `G.mass` and bumps **`G.massEarned`** (a lifetime tally / the "has ever accreted" flag the UI gates read; **now also drives the rising difficulty via `massUnit`**). `commitAccretion()`/`baseMassGain()`/`resetUniverse()` live in state.js.
- **Mass upgrade tree** (`upgrades/mass.js`, **10 wired nodes**, **few-but-powerful**; placeholders fill the deeper tiers): **Maw** - *Denser Core* (×pulse income, **+50%/lvl**, 5 lvls, costs [1,3,7,13,22], tier 1) · *Heavier Pulse* (**+10 ✦ per pulse/lvl**, added to the pulse base via `heavierPulseBonus()`, 5 lvls, costs [2,5,10,18,30], tier 1) · *First Light* (**tier 2**; start each universe with ✦ **1,000 → 5,000 → 20,000**, 3 lvls, costs [4,10,20]). **Orbiters** - *Heavier Bodies* (global ×orbiter payout, +50%/lvl, folded into `resonanceMult`, **2 lvls, costs [1,6]**) · *Dense Dust* (**+20 base payout per dust particle/lvl** via `denseDustBonus()` inside dust's `orbiterPayout`, 3 lvls, costs [2,6,13]) · *Lunar Favor* (×moon payout, +10%/lvl, 3 lvls, costs [1,4,9]; **moved here from Eternity**). **Phenomena** - *Brighter Tails* (×comet payout, +50%/lvl, 4 lvls, costs [1,3,7,13]) · *Comet Shower* (gap ×0.85^lvl = sooner, 3 lvls, costs [1,4,9]). **Eternity** (the accretion-cycle/time tab) - *Greater Collapse* (**×Mass/accretion = 1+0.1×lvl, 5 lvls**, costs [3,7,14,24,38]). Effects hook the live economy via the accessors in mass.js: `pulseValue` (Denser Core ×, **Heavier Pulse +flat**), dust `orbiterPayout` (**Dense Dust +flat**), `resonanceMult` (Heavier Bodies → every orbiter + comet combined-payout + gravpull), `moonPayout`/`moonAvgPayout` (Lunar Favor), `catchComet`/observatory comet value (Brighter Tails), `randCometGap` (Comet Shower), `massGain` (Greater Collapse). **First Light** applies at universe birth via `applyMassUniverseStart()` - called by `resetUniverse()` and re-called on every Mass-page buy; it sets `G.dust` only (not `runDust`/`totalDust`) so it can't farm Mass. (**Retained Companions was removed**; **Dense Dust** + **Heavier Pulse** are the new wired tier-1 nodes. **Cost philosophy:** first level 1-3 Mass, later levels ramp steeply to track the rising Mass-per-accretion.)
- **Tiers / progression — the Singularity spine (Branching Rail)**: the Mass page is the **Branching Rail** layout - a shared left **spine** (`SINGULARITY` in mass.js: `tiers`=8, `orbiters`=['Dwarf Planet','Comet Cluster','Ringed Body','Gas Giant','Pulsar','Companion Star','Quasar','Rogue Star'], `costs`=[1,10,20,32,48,70,100,140]) that runs down **every tab**. Buying the next spine level (`buySingularity()` → `G.massUpgrades.singularity`++) grows the Maw, **captures the next orbiter, and unlocks that tier's upgrades across all four categories**. `tierUnlocked(t)` = `singularityLevel() >= t`; `massNodeVis(u)` returns `'available'` if its tier is unlocked, else `'locked'` - so **NOTHING in any tab is buyable until the matching tier's orbiter is captured**. After every accretion the only purchasable thing is the spine's tier-1 gate, **Dwarf Planet (1 Mass)**; capturing it opens tier 1 across all tabs (the old prev-tier-maxed gating + `massCurrentTier`/`massTierMaxed` are gone). **No tier 0** - the 3 base orbiters are free; the wired upgrades are **tier 1** (Denser Core+Heavier Pulse / Heavier Bodies+Dense Dust+Lunar Favor / Brighter Tails+Comet Shower / Greater Collapse) and **tier 2** (First Light). The spine **tier-2 gate costs 10 Mass** (tier 1 = 1 Mass). `placeholder:true` nodes (Maw: Event Horizon, Ergosphere, Accretion Disk, Photon Sphere, Gravity Lens, Hawking Glow, Spaghettification; Orbiters: Swifter Orbits, Tidal Lock, Shepherd Moons, Resonant Chorus, Orbital Cascade, Captured Belt, Binary Dance; Phenomena: Meteor Shower, Aurora, Nebula Bloom, Solar Flare, Magnetar Pulse, Supernova Echo, Cosmic Ray; Eternity: Time Dilation, Eternal Return, Memory of Light, Long Now, Frozen Moment, Deep Time, Eternal Recurrence) fill tiers 2-5 (**multiple per tier**) - **not wired**, rendered as a dim **Planned** card and **never buyable** (`buyMassUpgrade` refuses `u.placeholder`). `singularity` persists in `G.massUpgrades` (added to `MASS_UPG_IDS`), surviving every accretion. **Four node states the rail shows:** **Completed** (`max`, gold fill) · **Owned-keep-upgrading** (`up`) · **Available / not yet bought** (`avail`, 0 levels, gold border) · **Locked** (tier's orbiter uncaptured - name + lock + "Capture [orbiter] to unlock") - plus the **Planned** placeholder preview. Each (tab × tier) cell can hold **multiple** cards. (The orbiter *bodies* aren't spawned in-universe yet - the spine currently gates the Mass tree; wiring Dwarf Planet etc. as real orbiters + their unique upgrades is the next step.)

## Git workflow
- All changes → commit and push to `refine/v.1`
- Merge to `main` only after user explicitly confirms a feature is good
- Remote: https://github.com/Jason-Jisu-Lee/around-the-cosmos.git (repo renamed from `maw`)

## Progressive upgrade unlock
- Each upgrade in `config.js` has an `unlock: () => boolean`; the card shows only when it returns true.
- `ui.js` computes a `visibleSig()` fingerprint each frame; when it changes (a new upgrade unlocks), the panel rebuilds automatically - so newly-unlocked cards appear without manual `buildPanels()` calls.
- To gate an upgrade behind a milestone, set its `unlock` fn, e.g. `unlock: () => lvl('touch') >= 2`.
- **Buying an upgrade only rebuilds the panel if `visibleSig()` changed** (else `updateCards()` refreshes values in place). A full rebuild recreates every card element and would kill an in-progress "new upgrade" highlight on a sibling - so card-value changes (e.g. leveling Cosmic Pulse) must NOT rebuild. `resetPanelAnimations()` (panels.js) clears `seenUpg` on a universe reset (debug Reset, future prestige) so re-unlocked cards flash again - needed because no page reload happens.

## Vibe coding rules
- Don't change CFG balance numbers or upgrade costs without being asked
- Don't add TypeScript, build tools, or external dependencies
- Visual aesthetic: parchment/editorial (bg #f4efe4, ink #1a1a1a, Georgia serif, flat/no-gradient shapes)
- User describes what they want in natural language; interpret generously
- After each change, push to `refine/v.1`
- Update this CLAUDE.md whenever the architecture, mechanics, or file structure changes
