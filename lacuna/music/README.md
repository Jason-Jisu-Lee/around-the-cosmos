# Music

Audio is procedurally generated via the Web Audio API in `../sound.js`.
No external files needed — everything runs in-browser.

To replace with real audio files later:
- Drop .mp3/.ogg files here
- Update sound.js to load them with `new Audio()` instead
